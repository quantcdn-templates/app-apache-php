; Memory Configuration
; Sets PHP memory limits for optimal performance
; Configurable via PHP_MEMORY_LIMIT environment variable (default: 256M)

; Maximum memory per script execution
memory_limit = ${PHP_MEMORY_LIMIT}