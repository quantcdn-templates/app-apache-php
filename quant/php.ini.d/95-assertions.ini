; Disable assertions for production use
; Note: assert.active is deprecated in PHP 8.3+ and removed in PHP 8.4
; Use zend.assertions instead (the modern way)

; -1 = do not compile assertions (production mode, best performance)
;  0 = compile but don't execute assertions
;  1 = compile and execute assertions (development mode)
zend.assertions = -1

